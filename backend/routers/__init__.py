"""HTTP routers — owned by the `backend-api` teammate.

Each router is wired into the FastAPI app in `backend/main.py`. Phase 0
ships with stubs so the app boots; the teammate replaces the stubs with
real implementations.
"""
